import 'package:flutter/widgets.dart';

class SinnersGame extends StatelessWidget {
  const SinnersGame({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Sinners'),
    );
  }
}
